<!DOCTYPE html>
<html>
  <head>
    <title><%= headTitle %></title>
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body>
    <%- include('partials/navigation', { menuLinks: menuLinks, activeLinkPath:
    activeLinkPath }) %>

    <main>
      <h1 class="heading">Newest product</h1>
      <% if (newestProduct && Object.keys(newestProduct).length > 0) { %>
      <ul class="product-list">
        <li class="product-item">
          <strong>Name:</strong> <%= newestProduct.name %><br />
          <strong>Description:</strong> <%= newestProduct.description %>
        </li>
      </ul>
      <% } else { %>
      <div style="text-align: left; padding-left: 40px">
        No new products available.
      </div>
      <% } %>
    </main>
  </body>
</html>
